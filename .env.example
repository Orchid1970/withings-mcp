# Withings API Credentials (Required)
# Obtain from https://developer.withings.com/ by creating an application
WITHINGS_CLIENT_ID=your_client_id
WITHINGS_CLIENT_SECRET=your_client_secret

# Database URL (Optional - defaults to SQLite for local development)
DATABASE_URL=sqlite+aiosqlite:///./withings.db

# Encryption Key (Required)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_fernet_key_here

# Base URL (Required for OAuth callback)
# Set to your public URL for production, or http://localhost:8000 for local development
BASE_URL=https://your-app.railway.app